# V1-00 baseline and author evidence — 2026-09-06

V1 is **not complete**. The application has substantial native functionality, but
real instrument assets, fixed-rate device edges, complete automatic song analysis,
application Lyria streaming, rig scheduling/clock, grounded review/trends,
onboarding and developer release acceptance remain. Historical milestone ticks
and old issue titles are not acceptance evidence.

This is a documentation-only reconciliation prepared for independent Opus 5
review. It changes no feature, dependency, provider configuration, architecture
decision or required check. No PR was merged, issue closed, release/asset published,
paid call made or other model launched. In particular, #274 remains open and
untouched. Author completion, independent review, merge and V1 acceptance are
separate states.

## Reproducible observation

- Observation started **2026-09-06T17:26:55.645616Z**. Main and author base:
  `6b3a4c6fe07838a3f69dd5db4c497d0d39884584`.
- Verified remote: [Josefines Jamstudio](https://github.com/avoidencez-lgtm/josefines-jamstudio).
  Isolated author branch: `docs/v1-00-reconciliation`, initially clean.
  Existing `feat/loopback-calibration` at
  `cb2f232a564f381fc5e1f39aab2e24aa87d9257d` and the alignment worktree were preserved.
- **42 open issues and 30 open PRs**, including new #275/#276 and #277/#278 since
  the supplied handoff's 40/28 count. REST `issues?state=open` excludes entries
  carrying `pull_request` when counting issues. Both lists, PR changed files,
  comments and reviews were paginated at 100 until the last short page.
- [Minimal snapshot](V1-SNAPSHOT-2026-09-06.json) records exact heads/bases,
  changed paths, full-diff SHA-256, check URLs, source ranges/hashes and the hashes
  of the three local handoff files. Raw API responses, full diffs, temporary
  reproductions, logs and the resumable checkpoint are kept outside the product
  tree. No machine-specific path or unrelated conversation is needed to use this PR.
- Baseline main [CI run 34043018498](https://github.com/avoidencez-lgtm/josefines-jamstudio/actions/runs/34043018498)
  passed all five jobs, including Windows/macOS headless suites, standalone S4
  checks and the built desktop's 25-second frontend handshake. This predates
  author changes and is **baseline evidence only**.
- GitHub main protection returned404 “Branch not protected”; repository rulesets
  returned `[]`. The release listing returned no releases. CI passing does not
  mean GitHub protection is configured or installers have shipped.

The final PR body records the immutable author commit, its exact-head CI URLs,
coverage result and the single closeout refresh. It is deliberately outside this
commit to avoid self-referential commit hashes. The self-documentation PR is
bookkeeping, not an omitted product PR from the earlier snapshot. Material
external changes at closeout require affected-row updates before author completion.

## Deliverables and coverage

1. [Requirements](V1-REQUIREMENTS-2026-09-06.md): **55 capability rows** with
   implementation, source/code/test evidence, remaining work, tolerances,
   verification limits, dependencies, risk, owner/reviewer and external prerequisites.
2. [Inventory](V1-INVENTORY-2026-09-06.md): each of the 42 issues and 30 PRs once,
   with dispositions, conflicts, overlap and exact-head evidence.
3. [Queue](V1-QUEUE-2026-09-06.md): **48 bounded sequential tasks**, named native
   model/harness, owned paths, reuse, real dependencies, acceptance, independent
   reviewer, fallback and stop boundary. A blocked provider gate does not block
   independent offline work.
4. [Snapshot](V1-SNAPSHOT-2026-09-06.json): **288 source units**, including every
   build-plan bullet/goal/acceptance/demo, S1–S5, CI specification, nine original
   DoD targets, twelve invariants, individual architecture numeric tests, all
   accepted ADRs, architecture sections and every EXTENDING heading/recipe.
   Unscheduled backlog and personal V2 checks are explicitly distinguished.

Coverage checks compare the live-snapshot numbers with the Markdown inventory,
extract R/T IDs and required fields from the authored Markdown, verify dependency
order and cross-references, independently enumerate source obligations from the
base commit, and compare every recorded source-range SHA-256. They check actual
source/test paths and local Markdown targets/anchors. Coverage is a structural
completeness check; independent review must still challenge factual classification
and whether a grouped capability faithfully covers every mapped clause.

## Verification performed for this assignment

All local verification used offline/headless settings and disposable test storage.
No local native test failure was repaired by changing unrelated product code.

| Check | Outcome and limit |
|---|---|
| Frozen pnpm install | Passed; existing lockfile, no dependency change |
| `pnpm lint`, `pnpm typecheck` | Passed; Biome195 files, TypeScript |
| `pnpm test` | Passed:45 files,279 tests. Store/preview evidence, not hardware |
| `pnpm licenses:check` | Passed:161 packages plus native vendor license/SHA checks |
| `pnpm build` | Passed |
| Workspace and S4 `cargo fmt --check` | Passed |
| Workspace and S4 Clippy/deny | Blocked by Windows Application Control, OS4551; executable never ran |
| `cargo test --workspace` and S4 tests | Blocked at dependency build scripts by OS4551; not claimed passed locally |
| Embedded native desktop build/smoke | Build blocked by the same policy; smoke not run locally |
| Gitleaks, offline Lychee, Lua, full Rust/native checks | Required on the final author commit in CI; baseline CI pass is separately linked above |
| Coverage/link/diff | Passed: 42/42 issues, 30/30 PRs, 55 rows, 288 source units, 48 tasks; no omissions/duplicates. Independently enumerated 143 build obligations, 35 extension sections, nine DoD targets and twelve invariants; source hashes, local links/anchors and SHA-pinned code paths checked |
| Ponytail author review | Repeated source permalinks reduced to coverage anchors; no shipped validator/framework/dependency. Remaining documentation is the requested audit scope. Independent Opus factual review is still pending |
| Physical, paid-provider, installed-model and subjective listening | Not performed; explicit product prerequisites, not documentation-author blockers |

The new snapshot initially failed Biome formatting; formatting that file repaired
the author-introduced failure. Final local lint passed196 files. The local Rust
limit is a host enforcement restriction, not an application
test failure or proof of correctness. It was not bypassed. No new local native
pass is inferred from historical logs. CI on this documentation PR must itself
be green before the author stops.

## Targeted suspect-PR checks

Five offline reproductions executed source extracted with `git show` at each
snapshot head. TypeScript used the installed compiler and Node's standard `vm`;
only the relevant helper/method and minimal state/deferred results were supplied.
They are **not mounted-UI or native integration tests**. No provider/model was called.

| PR | Actual source execution | Observed defect / required repair check |
|---|---|---|
| #241 | `setClickVolume` method; initial 0.5, request 0.6 then 0.4, both promises fail | Ends at 0.6 instead of confirmed 0.5. Also test late failure after newer success |
| #244 | Existing `studioFingerprint`/`songFingerprint` and new memo expression | Stored value wraps a song JSON string; memo wraps an object. Fresh proposal appears stale and Apply disables |
| #247 | Full parser with explicit `rock-straight` catalog and no live engine | `stop straight away` returns `set_style`, not Stop |
| #248 | Exact `videoSaveCopy`, compared with native `p.title.len() > 300` |148×`ø` is296 UTF-8 bytes; appending copy suffix produces303 and native save refuses |
| #264 | Exact `songQuery` helper | `"Blå natt".` retains surrounding quotes after period removal; expected title `Blå natt` |

All five assertions confirming these defects passed. Their exact heads and source
paths are in the inventory; the minimal inputs above allow an independent reviewer
to reproduce without the author's scratch files.

Additional findings are **source/review traces, not fresh native reproductions**:

- #255 stores a live reference serial on reference load. `band_load_chart` and
  `configure_song` directly clear the reference without resetting that atomic.
  Rendered chart frames have source serial0; OutputTap rejects them against the
  previous nonzero serial. An integration test must cover reference→chart/original
  and other buses, not just two manually tagged reference frames.
- #228 cuts notes before rendering the event's spans; #245 negative-jitter ownership
  must be tested through that same chronological path. A helper note-off test is
  insufficient for audio/MIDI parity across wraps.
- #232 cancellation can bypass the cost append after the awaited request; #234
  leaves a queued automatic tier after follow-energy is disabled; #249's timeout
  `?` exits before subprocess cleanup below its select. Preserve these as repair
  hypotheses with the stated checks, not fabricated runtime failures.
- #235's [failed Rust run](https://github.com/avoidencez-lgtm/josefines-jamstudio/actions/runs/33999913465)
  and #243's [failed TS run](https://github.com/avoidencez-lgtm/josefines-jamstudio/actions/runs/34013435094)
  were inspected. #243 has two Sessions error-message assertions still expecting
  `Error: ` prefixes. Main already has stronger export validation than #235's
  saturation approach; that portion is superseded, not permission to drop its
  remaining native library-validation requirement.

## Material unresolved V1 gates

- Fixed48 kHz internal clock/device edges and full callback/recording/MIDI continuity.
- Audited real kit/SoundFont files and delivery; placeholder manifest is not a pack.
- Recorded provider fixtures and supported current endpoint/model/rate/cap contracts.
  S4's 600-second synthetic loopback is not ten minutes with Lyria. S1 has no measured
  throughput evidence, so its PCM-over-IPC fallback cannot be assumed available.
- Developer known-stem guitar residual≤−6dB, musical listening, interface/xrun and
  physical calibration measurements; automatic analysis and trustworthy take review.
- Developer headset Jo 27/30 recorded actions and live 10-turn median≤2.5s; Lyria
  live 10-minute reconnect/gap evidence; actual selected accounts/harnesses unknown.
- Developer virtual MIDI, Windows installation and macOS `.dmg` installed startup;
  signing/notarisation remains a prerequisite until done or explicitly decided by
  the owner. Release publishing and branch-protection changes need their own authority.
- Every extension recipe and six no-core-edit fixture additions; full native error,
  accessibility, performance and end-to-end rehearsal.

Friend-led personal Mac/HeadRush/Black Spirit/Logic/subjective sessions in
[owner verification](06-owner-verification.md) remain **deferred V2, not passed**.
That deferral does not remove any developer gate or unfinished agreed V1 feature.
Mixed enhancement issues are separated in the inventory; optional housekeeping
does not become a new release gate merely because it has an issue.

## First execution handoff after independent review

T01 is the first slice: bounded Opus protocol investigation. It may finish its
offline contract/prerequisite report while live fixtures remain unavailable;
provider implementation must not rely on guessed shapes. Use this prompt only
after V1-00 independent review/corrections and explicit task dispatch:

```text
Execute T01 only for Josefines Jamstudio, using Opus 5 in Claude Code
(claude-opus-5; verify and record the installed harness version and selected model).
Work sequentially without subagents. Verify the remote and use an isolated,
freshly fetched main worktree; preserve all existing work and PR #274.

Read AGENTS.md, docs/plan/02-working-method.md, the reviewed V1-00 baseline,
requirements R25/R28/R31/R33/R51, snapshot and T01 queue entry, then architecture,
ADR0003/0004/0010 and S1/S3/S4/S5 findings plus the actual probe/provider callers.
The integrator must supply the reviewed V1-00 PR/head and current base SHA at dispatch.

Deliver one bounded documentation/contract investigation. Resolve contradictory
Lyria version/endpoint/auth/model/sample-rate/message/control/session-cap claims
using current official primary documentation. Compare each claim with the existing
Rust probe and clearly label synthetic, recorded and live evidence. Determine the
supported Music.ai workflow/module contract and ElevenLabs STT/TTS/voices/stems
fixture requirements. Identify instrument asset provenance prerequisites without
publishing/downloading an unaudited pack. Reference the existing research, do not
redo the model comparison or invent a new provider framework.

Acceptance: dated source-backed matrix, exact conflicting code/doc locations,
supported conclusions versus precise unknowns, scrubbed-fixture capture plan,
numeric audio/latency/residual acceptance and explicit account/spend/hardware
prerequisites. No synthetic response may be called a recorded provider response.
Do not enable the S1 fallback without its measured gate. Keep Rust owning
bytes/time/secrets, 48k internally, native output and Band/Lyria exclusivity.

No paid calls, key requests, model launches, feature implementation, architecture
changes, PR merges, issue closures or releases. Complete independent offline work
when access is missing. If repository edits/commit/push/PR are authorized at dispatch,
finish required coverage/link/security/license/build/native CI gates on the exact
head and repair your own failures; otherwise return the reviewable contract report.
Independent reviewer: Astra in Codex. Fallback: Fable 5.1 in Claude Code after two
unsupported repair cycles; prepare a handoff, never launch it. Return exact head/PR,
source/fixture evidence, check results and blocked prerequisites, then stop.
```
