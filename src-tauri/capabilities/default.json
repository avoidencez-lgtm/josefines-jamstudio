{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Local main window event subscriptions. Closing and quitting are confirmed through the app_exit command after the UI's guard; files, audio and networking remain in Rust.",
  "windows": ["main"],
  "permissions": ["core:event:allow-listen", "core:event:allow-unlisten"]
}
